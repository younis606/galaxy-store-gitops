apiVersion: apps/v1
kind: Deployment
metadata:
  name: galaxy-store
  namespace: default
  labels:
    app: galaxy-store
spec:
  replicas: 2
  selector:
    matchLabels:
      app: galaxy-store
  template:
    metadata:
      labels:
        app: galaxy-store
    spec:
      containers:
        - name: galaxy-store
          image: younis606/galaxy-store:ccfee3e8234f581a3b6e2d57fc419bf926096f43
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
          envFrom:
            - configMapRef:
              name: galaxy-store-config
apiVersion: v1
kind: Service
metadata:
  name: galaxy-store-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: galaxy-store
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
